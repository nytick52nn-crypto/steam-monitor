[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra
asyncio_mode = auto
filterwarnings =
    error::DeprecationWarning
markers =
    slow: long-running or sleep-based tests
    integration: uses real on-disk DB paths from .env (optional data)
